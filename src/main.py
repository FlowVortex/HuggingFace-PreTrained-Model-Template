"""
The `main.py` file is located under `src` and is separate from our project files.
It's typically used as a script to enable model pre-training or fine-tuning.

We usually write some specific `.sh` scripts under `scripts`, and then use these scripts to call our `main.py` file to start model pre-training or fine-tuning.
"""
